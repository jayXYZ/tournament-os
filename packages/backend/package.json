{
  "name": "@tournament-os/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Convex backend (schema, functions, generated API) shared by the web and mobile apps.",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "test": "vitest run",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit -p convex/tsconfig.json"
  },
  "dependencies": {
    "@convex-dev/rate-limiter": "^0.3.2",
    "@tournament-os/shared": "workspace:*",
    "convex": "catalog:"
  },
  "devDependencies": {
    "@convex-dev/eslint-plugin": "^2.0.0",
    "@edge-runtime/vm": "^5.0.0",
    "convex-test": "^0.0.53",
    "eslint": "^9.39.4",
    "typescript": "catalog:",
    "typescript-eslint": "^8.60.0",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
